# Agent Instructions

이 파일은 ScenePick 백엔드 저장소에서 작업하는 Agent를 위한 상시 지침이다.

1. 모든 작업을 시작하기 전에 [`RULES.md`](./RULES.md)를 끝까지 읽고 준수한다.
2. Issue 구현은 Issue URL이 전달되고 `codex-ready` 라벨과 필수 요구사항이 확인된 경우에만 시작한다.
3. 작업 브랜치는 기본적으로 최신 `origin/dev`에서 독립 worktree로 생성한다.
4. `dev` 또는 `main`에 직접 push하지 않는다.
5. DB, 인증·인가, 공개 API 호환성, 배포·운영 설정 등 위험 변경은 사용자 승인 전에 수행하지 않는다.
6. Handoff 전에 `RULES.md`에 정의된 필수 검증과 자체 리뷰를 수행한다.
7. 지침 충돌, 권한 부족 또는 안전한 진행이 불가능한 상황에서는 추측하지 말고 작업을 중단한 뒤 보고한다.

하위 디렉터리에 더 구체적인 `AGENTS.md`가 있다면 이 파일과 함께 적용한다. 더 구체적인 지침이 이 파일 또는 `RULES.md`와 충돌하면 변경을 시작하지 말고 사용자에게 확인한다.
