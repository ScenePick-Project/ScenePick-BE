--테스트용 데이터
CREATE TABLE simple_test
(
    id   NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1),
    name VARCHAR2(100)
);

INSERT INTO simple_test (name)
VALUES ('테스트데이터1');
select *
from simple_test;

-- 1. 작품 기본 정보 테이블
CREATE TABLE CONTENT
(
    CONTENT_ID       NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE            VARCHAR2(300)                       NOT NULL,
    POSTER_IMAGE_URL VARCHAR2(4000),
    SYNOPSIS         CLOB,
    CREATED_AT       TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT       TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. 작품별 장르 테이블
CREATE TABLE CONTENT_GENRES
(
    CONTENT_ID NUMBER REFERENCES CONTENT (CONTENT_ID) ON DELETE CASCADE,
    GENRE_NAME VARCHAR2(50),
    PRIMARY KEY (CONTENT_ID, GENRE_NAME)
);

-- 3. 작품 출연진 테이블
CREATE TABLE CONTENT_PERSON
(
    PERSON_ID         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CONTENT_ID        NUMBER NOT NULL,
    NAME              VARCHAR2(100),
    CHAR_NAME         VARCHAR2(100),
    PROFILE_IMAGE_URL VARCHAR2(4000),

    CONSTRAINT FK_CONTENT_PERSON_TO_CONTENT FOREIGN KEY (CONTENT_ID) REFERENCES CONTENT (CONTENT_ID) ON DELETE CASCADE
);

-- 4. 작품 에피소드 테이블
CREATE TABLE CONTENT_EPISODE
(
    EPISODE_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CONTENT_ID NUMBER NOT NULL,
    EPISODE_NO NUMBER,
    TITLE      VARCHAR2(300),
    SUMMARY    VARCHAR2(4000),

    CONSTRAINT FK_CONTENT_EPISODE_TO_CONTENT FOREIGN KEY (CONTENT_ID) REFERENCES CONTENT (CONTENT_ID) ON DELETE CASCADE

);

-- 5. 작품 테스트 데이터
INSERT INTO CONTENT (CONTENT_ID, TITLE, POSTER_IMAGE_URL, SYNOPSIS)
VALUES (1, '스위트홈', 'https://example.com/sweethome_poster.jpg', '은둔형 외톨이 고등학생 현수가 가족을 잃고 이사 간 아파트에서 겪는 기괴하고도 충격적인 이야기');


-- 기본정보
INSERT INTO CONTENT_GENRES (CONTENT_ID, GENRE_NAME)
VALUES (1, 'HORROR');
INSERT INTO CONTENT_GENRES (CONTENT_ID, GENRE_NAME)
VALUES (1, 'THRILLER');
INSERT INTO CONTENT_GENRES (CONTENT_ID, GENRE_NAME)
VALUES (1, 'ACTION');

-- 출연진
INSERT INTO CONTENT_PERSON (CONTENT_ID, NAME, CHAR_NAME, PROFILE_IMAGE_URL)
VALUES (1, '송강', '차현수', 'https://example.com/songkang.jpg');
INSERT INTO CONTENT_PERSON (CONTENT_ID, NAME, CHAR_NAME, PROFILE_IMAGE_URL)
VALUES (1, '고민시', '이은유', 'https://example.com/gominsi.jpg');

-- 에피소드
INSERT INTO CONTENT_EPISODE (CONTENT_ID, EPISODE_NO, TITLE, SUMMARY)
VALUES (1, 1, '1화', '철거 직전의 아파트, 그린홈으로 이사 온 지 일주일. 집에만 틀어박혀 지내던 현수는 무섭게 돌변한 주민을 목격한다. 내가 먼저 사라지려고 했는데, 세상이 먼저 망했다.');
INSERT INTO CONTENT_EPISODE (CONTENT_ID, EPISODE_NO, TITLE, SUMMARY)
VALUES (1, 2, '2화', '전염병일까? 국가재난경보가 발령되자 긴장하는 주민들. 괴물에게 물린 상욱을 가둬버린다. 현수는 위험에 처한 아이들을 모른 척할 수없다. 그래, 나가보자. 집 밖으로.');

-- 유저 테이블
CREATE TABLE USERS
(
    USER_ID   VARCHAR2(50)                NOT NULL,
    EMAIL     VARCHAR2(100)               NOT NULL,
    USERNAME  VARCHAR2(30)                NOT NULL,
    PASSWORD  VARCHAR2(150),
    PHONE     VARCHAR2(20)                NOT NULL,
    BAN_YN    VARCHAR2(1)  DEFAULT 'N',
    ROLE      VARCHAR2(10) DEFAULT 'USER' NOT NULL,
    CREATE_AT TIMESTAMP    DEFAULT SYSTIMESTAMP,
    UPDATE_AT TIMESTAMP,
    DEL_YN    VARCHAR2(1)  DEFAULT 'N'    NOT NULL,
    DELETE_AT TIMESTAMP,

    CONSTRAINT PK_USERS PRIMARY KEY (USER_ID),
    CONSTRAINT UK_USERS_EMAIL UNIQUE (EMAIL),
    CONSTRAINT CK_USERS_BAN_YN CHECK (BAN_YN IN ('Y', 'N')),
    CONSTRAINT CK_USERS_DEL_YN CHECK (DEL_YN IN ('Y', 'N'))
);
