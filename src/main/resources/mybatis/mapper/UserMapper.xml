<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.scenepickbe.user.dao.UserDao">
    <!--회원가입-->
    <insert id="insertUser">
        INSERT INTO USERS (USER_ID, EMAIL, USERNAME, PASSWORD, PHONE, ROLE, CREATED_AT)
        VALUES (#{vo.userId}, #{vo.email}, #{vo.username}, #{vo.password}, #{vo.phone}, #{vo.role}, SYSTIMESTAMP)
    </insert>

    <!--아이디 중복 확인-->
    <select id="existsByUserId" resultType="boolean">
        SELECT CASE
                   WHEN COUNT(*) > 0
                       THEN 1
                   ELSE 0 END
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <!--이메일 중복 확인-->
    <select id="existsByEmail" resultType="boolean">
        SELECT CASE
                   WHEN COUNT(*) > 0
                       THEN 1
                   ELSE 0 END
        FROM USERS
        WHERE EMAIL = #{email}
    </select>
</mapper>


